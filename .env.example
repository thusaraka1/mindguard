# Copy this file to .env and update values as needed
# cp .env.example .env  (Linux/Mac)
# Copy-Item .env.example .env  (PowerShell)

# PostgreSQL (Docker) - use this when running: docker-compose up -d postgres
DATABASE_URL="postgresql://mindguard:mindguard123@localhost:5434/mindguard"
